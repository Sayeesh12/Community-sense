# Create this file with placeholder values, NOT real values
cat > .env.example << EOF
VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
EOF

git add .env.example
git commit -m "Add .env.example for reference"
git push